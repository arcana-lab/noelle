#!/bin/bash

installDir

# Set the command to execute
cmdToExecute="noelle-load -load ${installDir}/lib/LoopSize.so -loopsize $@ -disable-output -noelle-min-hot=0"
echo $cmdToExecute ;

# Execute the command
eval $cmdToExecute 
