== PREREQUISITES ==
LLVM 5.0.0
GCC that supports C++ 14

To enable them on the Zythos cluster: 
  - source /project/llvm/5.0.0/enable
  - source /opt/rh/devtoolset-6/enable

== BUILD AND TEST SETUP ==
To build and run tests, simply run make at the top level
